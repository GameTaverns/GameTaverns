/**
 * ThemeApplicator is now a no-op.
 * All color theming has been removed from the platform.
 * Light/dark mode is handled entirely by next-themes + CSS variables in index.css.
 */
export function ThemeApplicator() {
  return null;
}
